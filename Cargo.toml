[package]
name = "rust"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.140"
clap = { version = "4.5.4", features = ["derive"] } # Use a recent stable version
colored = "2.1.0" # For colored terminal output
